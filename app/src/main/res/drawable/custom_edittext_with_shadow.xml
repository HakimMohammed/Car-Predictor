<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Background color -->
            <corners android:radius="10dp" /> <!-- Adjust corner radius as needed -->
        </shape>
    </item>

    <!-- Border Shadow -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Border color (same as background) -->
            <corners android:radius="10dp" /> <!-- Adjust corner radius as needed -->
            <stroke
                android:width="1dp"
            android:color="#888888" /> <!-- Border color -->
        </shape>
    </item>

    <!-- Main Shadow -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="#ffffff" /> <!-- Shadow color -->
            <corners android:radius="8dp" /> <!-- Adjust corner radius as needed -->
        </shape>
    </item>
</layer-list>
